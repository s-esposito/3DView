{
  "name": "3dviewer-demo",
  "version": "0.0.1",
  "description": "GitHub Pages demo for 3DViewer — drag-drop COLMAP reconstructions and mesh files to render them in the browser.",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "node esbuild.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.4.0"
  }
}
